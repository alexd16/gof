var expect = require('expect.js');
var Board = require('./board');

describe('Board', ()=> {
  var board;
  beforeEach(()=> board = new Board);
  
  describe('#add', ()=> {
    it('adds a live cell to the board in the given position')
  })
  describe('#neighbours', ()=> {
    it('returns the neighbour cells for a given position');
  })
});
